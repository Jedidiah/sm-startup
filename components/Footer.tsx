import { css } from "@emotion/react";

export function Footer() {
  return (
    <footer
      css={css`
        background-color: #333;
        color: #eee;
        padding: 4rem 4rem 8rem 4rem;
        min-height: 40vh;
      `}
    >
      footer
    </footer>
  );
}
